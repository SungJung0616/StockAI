# Local Data

This directory holds local development artifacts only.

- `raw/` contains immutable responses from approved data sources.
- `processed/` contains normalized or derived artifacts.

Real market datasets, licensed data, credentials, and generated outputs are not committed.
Only `.gitkeep` markers preserve the directory structure.
